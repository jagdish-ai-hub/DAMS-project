# Prod profile. All connection details come from the environment.
# SPRING_DATASOURCE_URL / SPRING_DATASOURCE_USERNAME / SPRING_DATASOURCE_PASSWORD,
# JWT_SECRET, APP_BASE_URL, CORS_ALLOWED_ORIGINS must all be set.
spring:
  jpa:
    show-sql: false
